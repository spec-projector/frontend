export enum IssueLabel {
    design = 'design',
    frontend = 'frontend',
    backend = 'backend'
}

export enum IssueState {
    opened = 'opened',
    closed = 'closed'
}

export enum IssueSystem {
    gitlab = 'GITLAB',
    github = 'GITHUB'
}
